import { createMuiTheme } from "@material-ui/core/styles";
export const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#d44d2f"
    },
    secondary: {
      main: "#1d83c6"
    },
    background: {
      //default: "#405069fc"
      default: "#2b2a2a"
    }
  },
  spacing: { unit: 4 }
});
