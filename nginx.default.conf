upstream graphql {
  server graphql:4000;
  keepalive 15;
}

upstream cellmine {
 server 10.0.0.20:9200;
 keepalive 15;
}

server {
 listen 2212;
 location / {
   proxy_pass    http://cellmine;
 }
}

server {
  server_name    localhost;
  access_log      /var/log/nginx/adm.acc.log;
  error_log       /var/log/nginx/adm.err.log;
  client_max_body_size    2G;

  listen [::]:443 ssl ipv6only=on;
  listen 443 ssl;

  ssl_certificate     /etc/nginx/alhena.crt;
  ssl_certificate_key /etc/nginx/alhena.key;

  ssl_protocols TLSv1 TLSv1.1 TLSv1.2;
  ssl_prefer_server_ciphers on;
  ssl_ciphers "EECDH+AESGCM:EDH+AESGCM:AES256+EECDH:AES256+EDH";
  ssl_ecdh_curve secp384r1;
  ssl_session_cache shared:SSL:10m;
  ssl_session_tickets off;
  ssl_stapling on;
  ssl_stapling_verify on;

  root   /usr/share/nginx/html;
  index  index.html index.htm;

  location / {
    try_files $uri /index.html;
  }

  location /secure {
     try_files $uri /index.html;
  }

 location /graphql {
    proxy_pass      http://graphql;
  }

  gzip on;
  gzip_comp_level    5;
  gzip_min_length    256;
  gzip_proxied       any;
  gzip_vary          on;
  gzip_types
  application/atom+xml
  application/javascript
  application/json
  application/ld+json
  application/manifest+json
  application/rss+xml
  application/vnd.geo+json
  application/vnd.ms-fontobject
  application/x-font-ttf
  application/x-web-app-manifest+json
  application/xhtml+xml
  application/xml
  font/opentype
  image/bmp
  image/svg+xml
  image/x-icon
  text/cache-manifest
  text/css
  text/plain
  text/vcard
  text/vnd.rim.location.xloc
  text/vtt
  text/x-component
  text/x-cross-domain-policy;

}
server {
    if ($host = www.shahcomponc.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


    if ($host = shahcomponc.com) {
        return 301 https://$host$request_uri;
    } # managed by Certbot


  listen          80;
  listen          [::]:80;
  server_name     www.shahcomponc.com shahcomponc.com;
    return 404; # managed by Certbot
}
